export function LoginPage() {
    return (
        <main>
            <h1>Login</h1>
            <div>
                <input type="text" placeholder="User" />
                <input type="text" placeholder="password" />
            </div>
        </main>
    )
}